# === Database ===
# Local dev (SQLite):
DATABASE_URL="file:./dev.db"
# Production (MySQL on Hostinger):
# DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DATABASE_NAME"

# === Auth.js ===
AUTH_SECRET="generate-a-random-32-char-string"
AUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST=true

# === App ===
APP_URL="http://localhost:3000"

# === Storage & Persistence (Hostinger / Production) ===
# Optional custom persistent directory outside git repo root (defaults to ../lms_storage)
STORAGE_DIR="../lms_storage"

# === Security ===
PDF_SIGNING_SECRET="generate-a-secure-random-string"

# === Seed (optional) ===
SEED_ADMIN_EMAIL="admin@tradelearninghub.local"
SEED_ADMIN_PASSWORD="ChangeMe123!"
SEED_ADMIN_NAME="Platform Admin"
